# Happy 内网版部署环境变量
#
# 复制此文件为 .env 并修改相关配置
# cp .env.internal.example .env

# JWT Secret (必填，生产环境请使用随机生成的强密码)
JWT_SECRET=your-secret-key-here

# 可选配置
# PORT=3005

# 以下为 PGlite 相关配置（默认即可）
# DB_PROVIDER=pglite
# PGLITE_DIR=/data/pglite

# 认证模式（默认 local，使用用户名密码 + JWT）
# HAPPY_AUTH_MODE=local
